namespace TaskConnect.TaskService.Domain.Models;
// Define a simple model for the summary response (adjust properties as needed)
public class WorkLogSummaryModel
{
    public double TotalHours { get; set; }
    // Add other summary properties here, e.g., count of worklogs, average duration, etc.
}